{
  "name": "travel-agent",
  "version": "0.3.0",
  "description": "AI Travel Planner Agent - LLM-powered trip planning with weather, attractions, and budget tools",
  "private": true,
  "scripts": {
    "dev:server": "npm --prefix server run dev",
    "dev:ui": "npm --prefix ui run dev",
    "dev": "concurrently -n server,ui -c blue,magenta \"npm run dev:server\" \"npm run dev:ui\"",
    "typecheck": "npm --prefix server run typecheck && npm --prefix ui run typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
